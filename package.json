{
  "name": "farmacovigia",
  "private": true,
  "version": "0.1.0",
  "description": "FarmacoVigía — agente de farmacovigilancia con rieles de pago x402",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w packages/shared -w packages/rxnorm -w packages/sources -w apps/worker",
    "test": "vitest run",
    "worker": "node apps/worker/dist/index.js",
    "worker:once": "node apps/worker/dist/index.js --once"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
